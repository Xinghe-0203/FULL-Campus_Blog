<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.edu_project.mapper.BlogTrendingMapper">

    <!-- 根据文章ID和日期查询趋势记录 -->
    <select id="selectByPostIdAndDate" resultType="com.example.edu_project.entity.BlogTrending">
        SELECT * FROM blog_trending
        WHERE post_id = #{postId}
          AND date >= #{dateStart}
          AND date &lt; #{dateEnd}
    </select>

    <!-- 批量根据文章ID和日期查询趋势记录（优化N+1问题） -->
    <select id="selectByPostIdsAndDate" resultType="com.example.edu_project.entity.BlogTrending">
        SELECT * FROM blog_trending
        WHERE post_id IN
        <foreach collection="postIds" item="postId" open="(" separator="," close=")">
            #{postId}
        </foreach>
          AND date >= #{dateStart}
          AND date &lt; #{dateEnd}
    </select>

    <!-- 获取热门文章列表（按热度评分降序） -->
    <!-- 修复：按 post_id 去重，只取每篇文章最近日期的记录，避免同一篇文章在7天内出现多次 -->
    <select id="selectHotPosts" resultType="com.example.edu_project.entity.BlogTrending">
        SELECT bt.* FROM blog_trending bt
        INNER JOIN (
            SELECT post_id, MAX(date) as max_date
            FROM blog_trending
            WHERE date >= #{dateStart}
              AND date < #{dateEnd}
            GROUP BY post_id
        ) latest ON bt.post_id = latest.post_id AND bt.date = latest.max_date
        ORDER BY bt.score DESC
    </select>

    <!-- 插入或更新趋势记录（使用 ON DUPLICATE KEY UPDATE 实现 upsert） -->
    <insert id="upsert" parameterType="com.example.edu_project.entity.BlogTrending">
        INSERT INTO blog_trending (post_id, score, view_count, like_count, comment_count, date, create_time, update_time)
        VALUES (#{trending.postId}, #{trending.score}, #{trending.viewCount}, #{trending.likeCount},
                #{trending.commentCount}, #{trending.statDate}, NOW(), NOW())
        ON DUPLICATE KEY UPDATE
            score = #{trending.score},
            view_count = #{trending.viewCount},
            like_count = #{trending.likeCount},
            comment_count = #{trending.commentCount},
            update_time = NOW()
    </insert>

    <!-- 获取所有文章的最新趋势数据（优化：使用 JOIN 替代相关子查询） -->
    <select id="selectAllLatest" resultType="com.example.edu_project.entity.BlogTrending">
        SELECT bt.* FROM blog_trending bt
        INNER JOIN (
            SELECT post_id, MAX(date) as max_date
            FROM blog_trending
            GROUP BY post_id
        ) latest ON bt.post_id = latest.post_id AND bt.date = latest.max_date
    </select>

</mapper>